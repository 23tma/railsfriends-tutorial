<nav class="navbar navbar-expand-lg bg-dark navbar-dark">
  <div class="container-fluid">
  	<%= link_to 'Friends App', root_path, class:"navbar-brand" %>
    <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
      <span class="navbar-toggler-icon"></span>
    </button>
    <div class="collapse navbar-collapse" id="navbarSupportedContent">
      <ul class="navbar-nav me-auto mb-2 mb-lg-0">
        <li class="nav-item">
          <%= link_to "About", home_about_path, class:"nav-link" %>
        </li>
        <% if user_signed_in? %>
          <li class="nav-item">
            <%= link_to "Add New Friend", new_friend_path, class:"nav-link" %>
          </li>
        <% end %>
      </ul>

      <% if user_signed_in? %>
        <div class="dropdown navbar-nav d-flex me-3">
          <button class="btn btn-outline-info dropdown-toggle" type="button" id="dropdownMenuButton1" data-bs-toggle="dropdown" aria-expanded="false">
            Profile
          </button>
          <ul class="dropdown-menu dropdown-menu-end" aria-labelledby="dropdownMenuButton1">
            <li><%= link_to "Account Settings", edit_user_registration_path, class:"dropdown-item" %></li>
            <li><%= link_to "Sign Out", destroy_user_session_path, :method => :delete, class:"dropdown-item text-danger" %></li>
          </ul>
        </div>
      <% else %>
        <div class="navbar-nav d-flex">
          <%= link_to "Sign In", new_user_session_path, class:"btn btn-outline-info me-3" %>
        </div>
      <% end %>
    </div>
  </div>
</nav>